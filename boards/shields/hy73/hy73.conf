# Keep logging/console off for the first memory feasibility build.
# Studio is enabled only for the LEFT build from build.yaml.
# The 74HC595 GPIO expander is SPI-backed, so every board using this shield needs SPI.
CONFIG_SPI=y
